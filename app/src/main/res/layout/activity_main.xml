<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <com.gmail.smjcrane.ultimateidle.EncouragementView
        android:layout_height="match_parent"
        android:layout_above="@+id/digitGrid"
        android:layout_width="match_parent" />

    <GridView
        android:id="@+id/digitGrid"
        android:numColumns="auto_fit"
        android:stretchMode="columnWidth"
        android:layout_centerInParent="true"
        android:columnWidth="40dp"
        android:verticalSpacing="-150dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/fact"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:text="That's more than the number of "
        android:textSize="30dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/digitGrid"/>



</RelativeLayout>